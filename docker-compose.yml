version: '3.8'

services:
    # PostgreSQL Database
    db:
        image: postgres:15-alpine
        container_name: smart_city_db
        volumes:
            - postgres_data:/var/lib/postgresql/data
        environment:
            POSTGRES_DB: smart_city_db
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
        ports:
            - '5432:5432'
        healthcheck:
            test: ['CMD-SHELL', 'pg_isready -U postgres']
            interval: 10s
            timeout: 5s
            retries: 5

    # Django Backend
    web:
        build: .
        container_name: smart_city_backend
        command: gunicorn --bind 0.0.0.0:8000 --workers 3 --timeout 60 core.wsgi:application
        volumes:
            - .:/app
            - static_volume:/app/staticfiles
            - media_volume:/app/media
        ports:
            - '8000:8000'
        env_file:
            - .env
        depends_on:
            db:
                condition: service_healthy
        restart: unless-stopped

    # Nginx (optional - for production)
    # nginx:
    #   image: nginx:alpine
    #   container_name: smart_city_nginx
    #   volumes:
    #     - ./nginx.conf:/etc/nginx/nginx.conf
    #     - static_volume:/app/staticfiles
    #     - media_volume:/app/media
    #   ports:
    #     - "80:80"
    #     - "443:443"
    #   depends_on:
    #     - web
    #   restart: unless-stopped

volumes:
    postgres_data:
    static_volume:
    media_volume:
